// Photos / memories domain — STUB owned by the Photos agent.
// Add the client slice (list/upload/album), types, and a usePhotos() hook here,
// mirroring goals.ts. The barrel already spreads `photosApi` into `api`.
export const photosApi = {}
