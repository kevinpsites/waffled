import { Placeholder } from './components/Placeholder'

// STUB — owned by the Meals agent. Replace with the real Meals planning /
// recipes screen per docs/handoff/screens-meals.js (KIOSK_meals).
export function Meals() {
  return <Placeholder title="Meals" icon="meals" />
}
