import { Placeholder } from './components/Placeholder'

// STUB — owned by the Photos agent. Replace with the real Photos / memories
// screen per docs/handoff/screens-lists.js (KIOSK_photos) + screens-extra.js.
export function Photos() {
  return <Placeholder title="Photos" icon="photos" />
}
