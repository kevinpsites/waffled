// The tick drawn inside a filled checkbox — the grocery board, Lists and cook mode
// all check things off the same way, so they draw the same mark. White stroke: it
// only ever sits on a filled (checked) box, never on the empty one.
export const CHECK = (
  <svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="#fff" strokeWidth="3">
    <path d="M5 12l5 5 9-10" />
  </svg>
)
