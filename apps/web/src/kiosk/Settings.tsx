import { Placeholder } from './components/Placeholder'

// STUB — owned by the Settings/Family agent. Replace with the real Settings &
// family-management screen per docs/handoff/screens-settings.js + settings2.js.
export function Settings() {
  return <Placeholder title="Settings" icon="settings" />
}
