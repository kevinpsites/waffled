import { Placeholder } from './components/Placeholder'

// STUB — owned by the Lists agent. Replace with the real Lists screen
// (custom lists beyond grocery) per docs/handoff/screens-lists.js (KIOSK_lists).
export function Lists() {
  return <Placeholder title="Lists" icon="lists" />
}
