import SwiftUI

/// Meals tab — placeholder for Phase 0. Weekly planner + recipe capture follow.
struct MealsView: View {
    var body: some View {
        TabPlaceholder(icon: "fork.knife", title: "Meals",
                       note: "This week's plan & recipe capture are coming up.")
    }
}
