import SwiftUI

/// Meals tab. Phase 1 lands the **Recipes library** (search/filter/sort) with a
/// drill-down to full recipe detail; the weekly planner + recipe capture follow.
struct MealsView: View {
    var body: some View {
        RecipesLibraryView()
    }
}
